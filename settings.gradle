// Not sure if this is strictly necessary at this stage
pluginManagement {
    repositories {
        mavenLocal()
        maven { url "https://repo.grails.org/grails/core/" }
        gradlePluginPortal()
    }
    plugins {
    }
}

rootProject.name="web-toolkit-ce"
